version: '3.8'

services:
  chat-room-frontend:
    build: .
    ports:
      - "3002:3002"
    environment:
      - REACT_APP_BASE_URL=http://localhost:3000/
      - REACT_APP_BASE_WS=ws://localhost:3001
      - PORT=3002